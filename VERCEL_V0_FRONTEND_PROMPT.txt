================================================================================
VERCEL V0 FRONTEND GENERATION PROMPT
Enterprise RAG Platform - Production-Ready Edition
100% Backend-Compatible
================================================================================

Create a futuristic AI platform frontend for an Enterprise RAG system with the following specifications:

=== DESIGN THEME ===
- Futuristic 2D line art comic/psychedelic aesthetic
- Neon gradient color palette: cyan (#00F0FF), electric magenta (#FF00FF), deep purple (#8B00FF), acid green (#39FF14)
- Dark base: #0A0A0F with elevated surfaces: #1A1A2E
- Bold 3-4px comic-style borders with animated gradient shifts
- Glassmorphism cards with backdrop-blur and neon glows
- 3D floating elements using Three.js (brain, geometric shapes, particles)
- Parallax scrolling and Framer Motion animations
- Glitch effects on titles and hover states
- Comic speech bubbles for notifications

=== TECH STACK ===
- Next.js 14 (App Router)
- TypeScript
- Tailwind CSS with custom neon theme
- Framer Motion for 2D animations
- Three.js + React Three Fiber for 3D elements
- React Hook Form for forms
- Zustand for state management
- TanStack Query (React Query) for API calls
- Axios for HTTP client
- Recharts for data visualizations
- shadcn/ui as base component library

=== PROJECT STRUCTURE ===
```
app/
  (landing)/
    page.tsx              # Landing page
  (dashboard)/
    layout.tsx            # Dashboard shell with sidebar
    documents/
      page.tsx            # Document management
    rag/
      page.tsx            # Search & Q&A
    analytics/
      page.tsx            # CSV analytics
    agent/
      page.tsx            # AI agent orchestrator
    ml/
      page.tsx            # ML predictions
    summarize/
      page.tsx            # Document summarization
    export/
      page.tsx            # Export reports
    health/
      page.tsx            # System health dashboard
components/
  ui/                     # shadcn components
  neon/                   # Custom neon components
  3d/                     # Three.js components
lib/
  api/
    client.ts             # Axios instance
    endpoints.ts          # API functions
  store/
    documents.ts          # Zustand store
    theme.ts              # Theme store
  types/
    api.ts                # TypeScript interfaces
```

=== PAGES & FEATURES ===

1. LANDING PAGE (/)
   - Hero with 3D animated brain (Three.js neural network)
   - Floating stat cards: "Documents Processed", "Queries Answered", "Insights Generated"
   - Feature grid (6 cards): Document Intelligence, RAG Search, CSV Analytics, AI Agent, ML Predictions, Export Reports
   - Each card: Icon, title, description, "Learn More" button
   - Animated tech stack logos carousel
   - CTA: "Get Started" (links to /documents)
   - No API calls on this page

2. DOCUMENTS PAGE (/documents)
   Components:
   - Drag-drop upload zone (react-dropzone)
   - File type badges: PDF, CSV, TXT, DOCX, MD
   - Upload progress bar with lightning animation
   - Document table with columns: Name, Type, Size, Uploaded Date, Status, Actions
   - Search input with debounce
   - Filter dropdown: All, PDF, CSV, TXT
   - Pagination controls
   - Action menu per row: Preview, Get Insights
   - Preview modal: Shows metadata + first 5 chunks
   
   API Integration:
   - POST /rag/ingest-file (FormData with file, source, chunk_size, overlap)
   - GET /rag/docs/list?limit=10&offset=0&status=success
   - GET /rag/docs/{id}/meta
   - GET /rag/docs/{id}/preview?max_chunks=5
   - GET /rag/supported-formats
   
   State:
   - TanStack Query for list with pagination
   - Optimistic updates on upload
   - Toast notifications for success/error

3. RAG SEARCH & Q&A PAGE (/rag)
   Components:
   - Tab switcher: [Search Mode] [Q&A Mode]
   - Large search input with AI icon animation
   - Top-K slider (1-20, default 5)
   
   Search Mode:
   - Results list with score bars (0-100%)
   - Color-coded confidence: >80% green, 50-80% yellow, <50% red
   - Truncated text with "Show more" expand
   - Source citation with document name
   
   Q&A Mode:
   - Chat-style interface (saved to localStorage)
   - Message bubbles: user (right), AI (left)
   - Typing indicator with animated dots
   - Citations list below each answer
   - Confidence badge on answers
   - Regenerate button
   
   API Integration:
   - POST /rag/query { query: string, top_k: number }
   - POST /rag/answer { question: string, top_k: number }
   
   Response handling:
   - Display meta.graceful_message if degradation_level exists
   - Show fallback indicators
   - Cache responses with React Query

4. CSV ANALYTICS PAGE (/analytics)
   Components:
   - Document selector dropdown (filtered to CSV files only)
   - Toggle: "Enable LLM Insights" (default: false)
   - Stats grid (4 columns):
     * Row count, Column count, File size
     * Mean, Median, Std Dev
     * Null percentage, Duplicate count
     * Outlier percentage
   - Data quality progress bars with neon styling
   - Interactive correlation heatmap (Recharts)
   - Distribution histograms for numeric columns
   - LLM Insights panel (collapsible, shows when enabled):
     * Dataset explanation
     * Key patterns (bullet list)
     * Relationships (bullet list)
     * Outliers & risks
     * Data quality commentary
   
   API Integration:
   - GET /rag/docs/list?status=success (filter to CSV)
   - GET /rag/analytics/csv/{document_id}?llm_insight_mode=false
   
   Visualizations:
   - Use Recharts with neon gradient fills
   - Animated bar charts
   - Responsive tooltips with glassmorphism

5. AI AGENT PAGE (/agent)
   Components:
   - Large prompt textarea (3 rows minimum)
   - Max iterations slider (1-10, default 5)
   - "Execute Agent" button with glow animation
   - Verbose mode toggle (shows extended metadata)
   - Real-time execution flow visualization:
     * Each iteration as comic panel
     * Tool name with icon
     * Arguments displayed in code block
     * Result preview
     * Success/error indicator
   - Final response card with:
     * Answer text
     * User message (friendly explanation)
     * Metadata: iterations, tools used, latency
     * Trace accordion (expandable steps)
   
   API Integration:
   - GET /agent/tools (show available tools on page load)
   - POST /agent/run?verbose=true { prompt: string, max_iterations: number }
   
   State:
   - Show loading spinner during execution
   - Disable button while running
   - Stream-like UI (show iterations as they complete in trace)

6. ML PREDICTIONS PAGE (/ml)
   Components:
   - Feature input form (dynamic 4 features for iris classifier)
   - Labels: "Sepal Length", "Sepal Width", "Petal Length", "Petal Width"
   - Number inputs with validation (min 0, max 10, step 0.1)
   - "Predict" button
   - Result card with:
     * Predicted class (large, animated reveal)
     * Probability bars (3 bars for 3 classes)
     * Color-coded by confidence
     * Metadata: Model name, latency, cache hit indicator
   - Example presets: "Iris Setosa", "Iris Versicolor", "Iris Virginica"
   
   API Integration:
   - POST /ml/predict { features: number[] }
   
   Response:
   - prediction: string (class name)
   - probabilities: number[] (3 values)
   - latency_ms_total, cache_hit, degradation_level

7. SUMMARIZATION PAGE (/summarize)
   Components:
   - Document selector (all formats supported)
   - Mode selector: Auto, Extractive, Hybrid
   - Summary length: Short, Medium, Detailed
   - Max chunks slider (1-20, default 5)
   - "Generate Summary" button
   - Result panel:
     * Summary text (large, readable)
     * Metadata card: Mode used, chunks used, confidence
     * Key sentences count (if extractive)
     * LLM used indicator
     * Latency metrics
   - Multi-document aggregation section:
     * Select 2+ documents
     * "Aggregate Insights" button
     * Shows per-document summaries + cross-document themes
   
   API Integration:
   - POST /rag/summarize { document_id, mode, max_chunks, summary_length }
   - POST /rag/insights/aggregate { document_ids: string[], mode, max_chunks }
   
   Aggregation response:
   - per_document: array of summaries
   - aggregated_insights: themes, patterns
   - Display as tabs or accordion

8. EXPORT PAGE (/export)
   Components:
   - Source type selector: RAG Answer, Summary, CSV Insights, Aggregation
   - Based on selection, show relevant input:
     * RAG: Select from recent Q&A (stored in localStorage)
     * Summary: Select document to summarize first
     * CSV: Select CSV document
     * Aggregation: Multi-select documents
   - Format toggle: Markdown / PDF
   - PDF availability check (call /export/capabilities on mount)
   - Filename input
   - "Generate Report" button
   - Preview panel (shows markdown before export)
   - Download button (appears after generation)
   
   API Integration:
   - GET /export/capabilities (check PDF support)
   - POST /export/report { payload_source, payload, format, filename }
   
   Response handling:
   - For MD: Display in preview panel with syntax highlighting
   - For PDF: Show "PDF generated" + download link
   - Handle degradation: If PDF fails, show MD fallback

9. HEALTH DASHBOARD PAGE (/health)
   Components:
   - System status cards (2x3 grid):
     * PostgreSQL: Status orb, latency, availability %
     * ChromaDB: Status orb, latency
     * LLM Services: Status orb, provider name
     * Redis Cache: Status orb, hit rate
     * Celery Workers: Count active/total
     * Ingestion: Success rate %
   - Status orb: Animated pulse, color-coded (green/yellow/red)
   - Ingestion health card:
     * Total documents, successful, failed
     * Success rate percentage
     * Total chunks
     * Avg processing time
   - Telemetry charts (Recharts):
     * API response times (line chart, last 24h)
     * Request volume (area chart)
     * Cache hit rates (donut chart)
   - Auto-refresh toggle + interval (default 5s)
   - Last updated timestamp
   
   API Integration:
   - GET /health (database status)
   - GET /rag/docs/health (ingestion statistics)
   
   Polling:
   - Use TanStack Query refetchInterval
   - Only when auto-refresh enabled
   - Show stale data indicator

=== GLOBAL COMPONENTS ===

Sidebar Navigation:
- Collapsible (hamburger icon)
- Logo at top
- Menu items: Home, Documents, RAG, Analytics, Agent, ML, Summarize, Export, Health
- Active indicator: Cyan glow
- Hover: Lift effect with shadow
- Icons: Use Lucide React icons

Top Navbar:
- Breadcrumb navigation
- Mock user avatar (no real auth)
- Client-side role toggle: "User Mode" / "Admin Mode" (localStorage)
- Theme switcher (dark is primary, light mode optional)
- In Admin mode: Show additional metrics

Toast System:
- Comic speech bubble style
- Types: Success (green), Error (red), Warning (yellow), Info (cyan)
- Auto-dismiss after 3s
- Stack in top-right
- Slide-in animation

Loading States:
- Full page: Orbital spinner with neon trails
- Inline: Pulse skeleton with shimmer
- Button: Mini spinner inside button

Error States:
- Friendly illustration (use undraw.co or similar)
- Error message
- "Try Again" button
- Suggestion text based on error type

=== API CLIENT SETUP ===

Environment variables:
- NEXT_PUBLIC_API_URL (default: http://localhost:8000)

Axios instance:
```typescript
import axios from 'axios';

export const apiClient = axios.create({
  baseURL: process.env.NEXT_PUBLIC_API_URL || 'http://localhost:8000',
  headers: { 'Content-Type': 'application/json' },
  timeout: 30000,
});

// Response interceptor for error handling
apiClient.interceptors.response.use(
  (response) => response,
  (error) => {
    const message = error.response?.data?.detail || error.message;
    toast.error(message);
    return Promise.reject(error);
  }
);
```

TanStack Query setup:
```typescript
import { QueryClient } from '@tanstack/react-query';

export const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 1000 * 60 * 5, // 5 minutes
      retry: 1,
      refetchOnWindowFocus: false,
    },
  },
});
```

=== TYPESCRIPT INTERFACES ===

Create types matching backend responses exactly:
```typescript
// Documents
interface Document {
  id: string;
  filename: string;
  source: string;
  format: string;
  size: number;
  created_at: string;
  status: 'success' | 'failed' | 'processing';
  chunk_count: number;
}

interface DocumentMeta {
  id: string;
  filename: string;
  format: string;
  chunk_count: number;
  ingestion_time_ms: number;
  // ... all fields from backend
}

// RAG
interface SearchResult {
  chunk: string;
  score: number;
}

interface AnswerResponse {
  answer: string;
  citations: Array<{
    chunk: string;
    page?: number;
    source?: string;
  }>;
  used_chunks: number;
  meta: {
    mode?: string;
    confidence_top?: number;
    provider?: string;
    latency_ms_retrieval: number;
    latency_ms_llm: number;
    cache_hit: boolean;
    graceful_message?: string;
    degradation_level?: string;
  };
}

// CSV Analytics
interface CSVInsights {
  summary: {
    row_count: number;
    column_count: number;
    file_size_mb: number;
  };
  column_profiles: Record<string, any>;
  data_quality: {
    null_percentage: number;
    duplicate_count: number;
    outlier_percentage: number;
  };
  llm_insights?: {
    enabled: boolean;
    mode: string;
    dataset_explanation?: string;
    key_patterns: string[];
    relationships: string[];
    outliers_and_risks: string[];
  };
  meta: {
    latency_ms_total: number;
    cache_hit: boolean;
  };
}

// Agent
interface AgentResponse {
  answer: string;
  user_message: string;
  trace: Array<{
    iteration: number;
    tool: string;
    arguments: Record<string, any>;
    result: string;
  }>;
  iterations: number;
  agent_meta: {
    decision_route?: string;
    tool_selected?: string;
    iterations: number;
    latency_ms_agent_total: number;
  };
}

// ML
interface PredictResponse {
  prediction: string;
  probabilities: number[];
  latency_ms_total: number;
  model_name: string;
  cache_hit: boolean;
  degradation_level?: string;
}
```

=== RESPONSIVE DESIGN ===
- Mobile-first approach
- Breakpoints: sm (640px), md (768px), lg (1024px), xl (1280px)
- Sidebar: Full on desktop, overlay on mobile
- Tables: Horizontal scroll on mobile with sticky first column
- Charts: Reduce height on mobile, stack vertically
- Forms: Full width on mobile, max-w-md on desktop

=== MOCK DATA MODE ===
Create a toggle in settings to switch between real API and mock data for demo purposes:
- Store in localStorage: 'useMockData'
- Mock responses for each endpoint
- Include realistic latencies (200-500ms)
- Toast notification: "Using mock data for demo"

=== ACCESSIBILITY ===
- ARIA labels on all interactive elements
- Keyboard navigation (Tab, Enter, Escape)
- Focus indicators with neon outline
- Screen reader-friendly text
- Semantic HTML (main, nav, article, aside)
- Alt text on all images/icons

=== PERFORMANCE ===
- Code splitting per route
- Lazy load 3D components (React.lazy)
- Image optimization (Next.js Image)
- Debounce search inputs (300ms)
- Memoize expensive computations
- Virtual scrolling for large lists (react-window)

=== DATABASE AWARENESS ===
The backend uses PostgreSQL for:
- Document metadata storage
- Ingestion history
- Telemetry logs

ChromaDB for:
- Vector embeddings
- Semantic search

This means:
- Document listings come from PostgreSQL (fast, paginated)
- Search results come from ChromaDB (vector similarity)
- Analytics read from both sources
- Health checks verify both database connections

Show database status indicators in health dashboard.

=== FILE STRUCTURE ===
Generate complete project with:
- package.json with all dependencies
- tsconfig.json
- tailwind.config.ts with custom neon theme
- next.config.js
- .env.example
- All page components
- All UI components
- API client utilities
- Type definitions
- Zustand stores
- README.md with setup instructions

=== ANIMATION DETAILS ===
- Page transitions: Fade + slide (150ms)
- Card hover: Lift 4px, shadow increase, border glow
- Button hover: Scale 1.05, glow pulse
- Loading: Smooth spin, no jank
- Toast: Slide in from right (300ms), bounce effect
- Modal: Scale up from 0.95 to 1, backdrop fade
- List items: Stagger animation on mount (50ms delay each)

=== COLOR VARIABLES ===
```css
:root {
  --neon-cyan: #00F0FF;
  --neon-magenta: #FF00FF;
  --neon-purple: #8B00FF;
  --neon-green: #39FF14;
  --hot-pink: #FF006E;
  --base-bg: #0A0A0F;
  --surface-bg: #1A1A2E;
  --text-primary: #FFFFFF;
  --text-secondary: #B4B4C8;
  --border-default: rgba(0, 240, 255, 0.3);
  --border-active: rgba(0, 240, 255, 1);
  --glow-cyan: 0 0 20px rgba(0, 240, 255, 0.5);
  --glow-magenta: 0 0 20px rgba(255, 0, 255, 0.5);
}
```

=== FINAL INSTRUCTIONS ===
Create a production-ready, visually stunning, fully functional frontend that integrates seamlessly with the FastAPI backend. Focus on performance, accessibility, and user experience. Make it portfolio-worthy!

All API endpoints are verified and compatible with the backend.
All TypeScript interfaces match backend response schemas.
All features are backed by real backend capabilities.

This is a complete, deployment-ready specification.

================================================================================
END OF PROMPT
================================================================================